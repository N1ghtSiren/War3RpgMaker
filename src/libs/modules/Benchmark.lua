---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Siren.
--- DateTime: 31.05.2023 17:37
---

---@class benchmark
---@field started_at number


function NewBenchmark()

  ---@type benchmark
  local benchmark
  benchmark = --[[---@type benchmark]]{}
  benchmark.started_at = os.clock()

  function benchmark:start()
    self.started_at = os.clock()
  end

  ---@return number
  function benchmark:getElapsed()
    return os.clock() - self.started_at
  end
  --- prints string .. "0.xxx seconds"
  ---@param str string
  function benchmark:finish(str)
    local elapsed = os.clock() - self.started_at
    printdf("%s %.3f seconds", str, elapsed)
  end

  --"initialized %s items in %.3f seconds"

  return benchmark
end

